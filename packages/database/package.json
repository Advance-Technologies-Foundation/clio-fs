{
  "name": "@clio-fs/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": { ".": { "types": "./src/index.ts", "import": "./dist/index.js" } },
  "dependencies": {
    "@clio-fs/contracts": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "node -e \"console.log('@clio-fs/database has no dev server')\"",
    "test": "corepack pnpm build && node --test dist/**/*.test.js",
    "format": "node -e \"console.log('No formatter configured for @clio-fs/database yet')\""
  }
}
