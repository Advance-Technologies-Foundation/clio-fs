{
  "name": "@clio-fs/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "node --watch src/index.ts",
    "test": "node ../../scripts/run-package-tests.mjs",
    "format": "node -e \"console.log('No formatter configured for @clio-fs/client yet')\""
  },
  "dependencies": {
    "@clio-fs/config": "workspace:*",
    "@clio-fs/contracts": "workspace:*",
    "@clio-fs/sync-core": "workspace:*"
  }
}
