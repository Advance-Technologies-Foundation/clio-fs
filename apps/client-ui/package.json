{
  "name": "@clio-fs/client-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "node ../../scripts/dev-compiled.mjs",
    "test": "corepack pnpm build && node --test dist/**/*.test.js",
    "format": "node -e \"console.log('No formatter configured for @clio-fs/client-ui yet')\""
  },
  "dependencies": {
    "@clio-fs/client": "workspace:*",
    "@clio-fs/config": "workspace:*",
    "@clio-fs/contracts": "workspace:*",
    "@clio-fs/ui-kit": "workspace:*"
  }
}
